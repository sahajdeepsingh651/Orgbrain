# Data Passport — Stack & Build Log

> Status: **LIVING DOC.** Two jobs, kept separate:
> 1. **§1 Stack** — the current, concrete tech choices, one row per component. No reasoning/trade-off prose here — that belongs in `decisions-log.md`; this table just says what we're using.
> 2. **§3 Build Log** — appended to every time something is actually implemented. A row only goes in once code exists and runs, not when something is merely planned.
>
> Update this file in the same commit/session as the work it describes — don't reconstruct it later from memory.

## 1. Stack by component

| Layer / component | Tech | Status | Defined in |
|---|---|---|---|
| Bronze (raw storage) | Folder structure or MinIO (S3-compatible object storage) | Decided | `data-passport-architecture.md` §3 |
| Silver (structured records) | PostgreSQL | Decided | `data-passport-architecture.md` §3 |
| Gold (search index) | PostgreSQL + `pgvector` extension, HNSW index (known gap: access-control filter applied post-candidate-selection, not before — see decisions-log.md) | Decided | `data-passport-architecture.md` § Gold, `decisions-log.md` (2026-08-07) |
| Embeddings | `fastembed` (ONNX runtime, no torch/GPU), model `BAAI/bge-small-en-v1.5`, 384 dims — fully local, no external API calls | Decided | `decisions-log.md` (2026-08-08, reversed the earlier hosted-API pick) |
| Context Bus | Postgres table (`context_bus_events`) + `LISTEN`/`NOTIFY` | Decided | `data-passport-core-service.md` §4 |
| Ingest API | REST, single `POST /v1/ingest`, synchronous (no queue/worker), Python + FastAPI, Postgres via `asyncpg` | Decided | `data-passport-core-service.md` §3, `decisions-log.md` (2026-08-08) |
| Serving API | REST (`/v1/search`, `/v1/agent-activity`, `/v1/handoff`) + MCP wrapper + SSE (`/v1/bus/subscribe`); same Python/FastAPI service; MCP via the official Python `mcp` SDK | Decided | `data-passport-core-service.md` §5, `decisions-log.md` (2026-08-08) |
| Endpoint Checkpoint (PII/secret detection) | Regex + gitleaks-style patterns (secrets); regex + lightweight NER e.g. Presidio (PII) | Decided (approach) — **Open**: runtime/language; owned by whichever endpoint mechanism gets built | `data-passport-architecture.md` § The Endpoint Checkpoint |
| Endpoint capture/consumption mechanism | Browser extension / network proxy / IDE plugin / manual dashboard entry | **Open** — not yet chosen | `data-passport-security-egress.md` §4–5 |
| Dashboard | React + Vite + TypeScript | Decided (not yet built — outside the core-service build order) | `decisions-log.md` (2026-08-08) |
| Auth | Bearer token, mapped to a fixed caller identity (`user_id`/`department`/`team`) via a static `config/api_tokens.json` file (`backend/app/auth.py`) | Decided | `data-passport-core-service.md` §3, `decisions-log.md` (2026-08-08) |
| Deployment | Docker Compose (app container + `pgvector/pgvector` Postgres image), single VM | Decided | `decisions-log.md` (2026-08-08) |

## 2. Open stack decisions (blocking implementation)

1. ~~Backend language/framework for the ingest + serving API.~~ — resolved 2026-08-08: Python + FastAPI (confirmed with the team).
2. ~~Embedding provider/model.~~ — resolved 2026-08-08: `fastembed` / `BAAI/bge-small-en-v1.5` (384 dims), local/on-prem, no external API (initially picked a hosted third-party embedding API, reversed same day per explicit no-external-API requirement — see `decisions-log.md`).
3. ~~Frontend framework for the dashboard.~~ — resolved 2026-08-08: React + Vite + TypeScript (confirmed with the team; not yet built).
4. ~~MCP server SDK.~~ — resolved 2026-08-08: official Python `mcp` SDK (follows from the backend-language choice).
5. ~~Deployment target — bare VM, Docker Compose, etc.~~ — resolved 2026-08-08: Docker Compose (confirmed with the team).
6. Endpoint-side runtime — whatever language the browser extension / proxy / IDE plugin ends up in; blocked on the interception-mechanism decision itself (`data-passport-security-egress.md` §5), not on this doc.

## 3. Build log

| Date | Component | What was built | Tech / library (+ version) | Where (path / PR) | Notes |
|---|---|---|---|---|---|
| 2026-08-08 | Postgres schema (build step 1) | `knowledge_entries`, `redaction_audit_log`, `agent_activity`, `context_bus_events`, `knowledge_embeddings` tables, `vector` extension, HNSW index on `knowledge_embeddings.embedding`, filter indexes on department/team/visibility/agent_id/session_id/created_at. Idempotent migration script; local Postgres via Docker Compose (`pgvector/pgvector:pg16`). Verified beyond the happy path: migration runs clean and is idempotent (ran twice); all 5 CHECK constraints (`visibility`, `status`, `outcome`, `consent_basis`, `redaction_audit_log.outcome`) reject invalid values and a missing required field (`title`) is rejected by NOT NULL; JSONB array/object columns default to `[]`/`{}` when omitted; `ON DELETE CASCADE` from `knowledge_entries` to `knowledge_embeddings` actually removes the child row; `ON DELETE SET NULL` from `knowledge_entries` to `agent_activity.record_id` actually nulls instead of deleting/erroring; `redaction_audit_log` accepts both `committed` and `quarantined` rows; the `agent_activity` upsert (`ON CONFLICT (agent_id) DO UPDATE`) replaces the existing row instead of duplicating; an HNSW similarity query (`ORDER BY embedding <=> ... LIMIT 5`) returns correctly ranked results and `EXPLAIN` confirms the planner uses `knowledge_embeddings_hnsw_idx` (not a sequential scan). All test rows cleaned up afterward — tables verified empty. | PostgreSQL 16 + `pgvector` (via `pgvector/pgvector:pg16` Docker image), `asyncpg` 0.30.0, `python-dotenv` 1.0.1 | [`backend/db/schema.sql`](../backend/db/schema.sql), [`backend/db/migrate.py`](../backend/db/migrate.py), [`docker-compose.yml`](../docker-compose.yml) | Host Postgres port is 5433, not 5432 — this dev machine already has a native Postgres service bound to 5432; see comment in `docker-compose.yml`. `agent_activity` is keyed by `agent_id` (one current-activity row per agent) — history lives in `knowledge_entries`, not here. |
| 2026-08-08 | Local embeddings (revised `knowledge_embeddings.embedding` from `vector(1536)` to `vector(384)`) | Switched from a hosted third-party embedding API to `fastembed` (see `decisions-log.md` reversal entry) for a fully on-prem embedding pipeline. Dropped and recreated the (still-empty) `knowledge_embeddings` table with the new dimension via `migrate.py`; re-verified column type, default `embedding_model`, FK, and HNSW index all intact. Ran a genuine end-to-end semantic test (not just a dimension check): embedded 3 real sentences (two about a cat/feline, one about quarterly revenue) plus a query sentence, inserted through `asyncpg`, queried via `<=>` through the HNSW index — the two semantically related sentences ranked closer to the query than the unrelated one (distances 0.1495 / 0.2653 vs 0.6663), confirming the model's output is actually meaningful, not just dimensionally correct. Test rows cleaned up afterward. | `fastembed` 0.8.0, model `BAAI/bge-small-en-v1.5` (384 dims, ONNX runtime, downloads once from Hugging Face then runs fully offline) | [`backend/db/schema.sql`](../backend/db/schema.sql), [`backend/requirements.txt`](../backend/requirements.txt) | First run downloads ~130MB of model weights (one-time, cached); confirmed a second run loads from cache in ~0.25s with no network call. |
| 2026-08-08 | `POST /v1/ingest` with stub Gate (build step 2) | FastAPI app: bearer-token auth (rejects before any Bronze/DB write — verified), unconditional raw-payload write to Bronze (`bronze/{team}/{source_system}/{yyyy-mm-dd}/{id}.json`) before validation runs, stub Gate (required-field + enum validation, `domain_data` type validation against `schemas/domains/*.json` per schema.md §4.0, provenance tagging, `consent_basis`/`consent_actor` stamping), real local embedding computation via `fastembed`, commit to `knowledge_entries` + `knowledge_embeddings` + `redaction_audit_log` in one transaction. Ingest contract extended per `decisions-log.md` (caller supplies `knowledge.{title,summary,outcome,...}` directly — no NLP extraction in the stub). Verified against a live running server (`uvicorn`, real HTTP requests, not an in-process test client): 2 happy-path commits (one with `domain_data`, one minimal) with DB rows and Bronze files checked field-by-field; 18 distinct validation-failure cases (missing fields, bad enums, bad `content` type, domain/`domain_data` presence mismatches, unknown domain, bad URL/enum/array-type inside `domain_data`, undeclared `domain_data` field) each confirmed to return 422 + a `quarantined` audit-log row + zero `knowledge_entries` rows; unit-tested the `number`/`boolean`/unknown-declared-type branches in the domain validator directly (including that Python `bool` is correctly rejected as `number` despite being an `int` subclass) since no shipped domain schema exercises them yet; confirmed auth failure happens before the Bronze write (file count unchanged, marker string absent). All test rows/files cleaned up afterward — DB tables and `bronze/` verified empty. | FastAPI 0.141.1, `uvicorn` 0.52.1, `asyncpg` 0.30.0, `fastembed` 0.8.0 | [`backend/app/main.py`](../backend/app/main.py), [`backend/app/domains.py`](../backend/app/domains.py), [`schemas/domains/engineering.v1.json`](../schemas/domains/engineering.v1.json) | Only `engineering.v1` domain schema exists so far — support/sales/data-ml domains from `data-passport-schema.md` §4 aren't built, add as `schemas/domains/{domain}.json` when needed. `agent_activity` is NOT populated by ingest — deferred to whichever build step first needs to read it. Context Bus write/`NOTIFY` is step 3, built separately below. |
| 2026-08-08 | Context Bus write + `NOTIFY` on every commit (build step 3) | Added a `context_bus_events` insert (compact projection: record_id/session_id/department/team/agent_id/event_type/title/summary/status/visibility/gold_ref) plus `SELECT pg_notify('context_bus', <json>)`, both inside the same transaction as the step-2 commit — Postgres defers `NOTIFY` delivery until `COMMIT`, so a rolled-back/quarantined request never fires one. `event_type` is hardcoded `'created'` for now (this endpoint only ever creates new records; no update endpoint exists yet to justify `updated`/`completed`/`handed_off`). Verified against the live server with a real `LISTEN` client (`asyncpg.add_listener`), not just a DB-side row check: (1) a committed ingest produces a `context_bus_events` row matching the record field-by-field; (2) a quarantined ingest produces zero bus-event rows; (3) the `LISTEN` client actually receives the `NOTIFY` and its JSON payload matches the committed record; (4) the replay/catch-up query (`WHERE created_at > checkpoint`) returns exactly the new event, no more, no less. Caught and fixed a real environment issue along the way: `uvicorn --reload`'s file-watcher was firing but never actually respawning the worker process on this Windows/Git-Bash setup (log showed only one "Started server process" ever, despite repeated "Reloading..." messages) — requests were silently being served by stale pre-edit code with no error. Dropped `--reload`; the dev server is now restarted manually after every code change. | Same as build step 2 (FastAPI, `asyncpg`) — no new dependencies | [`backend/app/main.py`](../backend/app/main.py) | Don't trust `--reload` in this environment — always confirm "Started server process" appears again in the log after an edit, or just restart manually. |
| 2026-08-08 | `GET /v1/search`, `/v1/agent-activity`, `/v1/handoff/{session_id}` (build step 4) | New `backend/app/auth.py` (token→identity map, replaces step-2's single shared `INGEST_API_TOKEN`; `POST /v1/ingest` now uses the same dependency) and `backend/app/serving.py` (all three endpoints, `APIRouter`). `/v1/search`: hybrid ranking — an HNSW ANN candidate set (`LIMIT 50`) unioned with a plain `ILIKE` keyword-match candidate set, re-ranked by vector distance, with visibility + optional `department`/`team` narrowing applied only to that final candidate pool (preserves the documented HNSW/access-control gap — did not "fix" it with an unindexed exact scan). `/v1/agent-activity`: derived from `knowledge_entries` (`DISTINCT ON (agent_id) ... ORDER BY created_at DESC`), see decisions-log.md — no `announce_task` write path exists to populate the `agent_activity` table itself. `/v1/handoff/{session_id}`: latest `knowledge_entries` row for that session, visibility-filtered in the same query so "not found" and "not visible" both collapse to a plain 404 (doesn't leak existence). Verified against the live server with a 5-record, 3-identity dataset (`dev`/Engineering-platform, `eng2`/Engineering-mobile, `sales`/Sales-enterprise) spanning all 4 visibility levels: full visibility matrix confirmed correct for all 3 callers on `/v1/search`; department/team query-param narrowing confirmed (narrows, never widens, what the identity is already allowed to see); semantic relevance confirmed (a "push notifications" query ranks the actual mobile-team record about push notifications first, not just returns *a* result); `/v1/agent-activity` visibility + `team` filter confirmed; `/v1/handoff` confirmed for own-private/org/cross-department-403-as-404/nonexistent-as-404; confirmed all three reject unauthenticated requests with 401; confirmed JSONB columns (`domain_data`, `key_points`, etc.) round-trip as real nested JSON in responses, not double-encoded strings — `asyncpg` does not auto-decode `jsonb` by default, verified directly before writing the response-building code. Also re-ran the step-2 ingest tests against the refactored auth to confirm no regression. Two test-harness mistakes caught and corrected, not app bugs: an early `agent-activity` assertion failed only because a prior manual test's leftover row was still in the DB; cleanup for `context_bus_events`/`redaction_audit_log` (append-only, no cascade from `knowledge_entries` deletes by design) needed to be explicit. All test data cleaned up afterward — every table verified empty. | Same as build steps 2–3 (FastAPI, `asyncpg`, `fastembed`) — no new dependencies | [`backend/app/auth.py`](../backend/app/auth.py), [`backend/app/serving.py`](../backend/app/serving.py), [`backend/app/embeddings.py`](../backend/app/embeddings.py), [`config/api_tokens.example.json`](../config/api_tokens.example.json) | `?project=` on `/v1/agent-activity` is accepted by nothing — no `project` field exists anywhere in the schema (checked `data-passport-schema.md`), so it's currently a silent no-op; only `team` actually filters. `config/api_tokens.json` (the real, non-example file) is gitignored. |
| 2026-08-08 | `GET /v1/bus/subscribe` (SSE, build step 5) | Added to `backend/app/serving.py`. Long-lived `StreamingResponse`: on connect, replays `context_bus_events` since a `since` query param or (native `EventSource` reconnect) `Last-Event-ID` header — defaults to "now" (live-only, no replay) if neither given — then holds one pooled `asyncpg` connection for the rest of the stream's life, `LISTEN`ing on `context_bus` and bridging the callback into the async generator via an `asyncio.Queue`; a 15s `asyncio.wait_for` timeout on the queue doubles as both an SSE keepalive comment and a `request.is_disconnected()` poll interval. Mandatory identity-based visibility filtering applies to both the replay query and every live event (Python-side for live events, since Postgres broadcasts a `NOTIFY` to every listener on the channel unfiltered); optional `department`/`team`/`visibility` query params narrow further, same as `/v1/search`. Verified against the live server: replay via `since` delivers exactly the missed event; a live event reaches a subscriber whose identity matches (department+team) and is withheld from one who shouldn't see it; `department`/`team` narrowing confirmed; unauthenticated connection attempts get 401; ran 8 back-to-back open-and-abandon connection cycles and confirmed via `pg_stat_activity` that the Postgres connection count returned to baseline every time — no leaked listeners/connections. One real gap found and documented rather than silently patched: `private`-visibility events are never delivered to anyone over the bus, not even the author (see `decisions-log.md` — the compact bus projection has no `author_user_id` to check "is this mine" against). | Same as build steps 2–4 — no new dependencies | [`backend/app/serving.py`](../backend/app/serving.py) | Each open SSE connection holds one connection from the shared `asyncpg` pool for its entire lifetime — with the pool's default size, a handful of concurrent dashboard tabs could exhaust it and start blocking ingest/search requests. Fine at hackathon-demo scale; revisit (dedicated listener connection pool, or a pool-size bump) if the dashboard opens many simultaneous subscriptions. |
| 2026-08-08 | MCP tool wrappers around step 4's endpoints (build step 6) | `backend/app/serving.py`'s three route handlers were refactored to thin FastAPI-specific wrappers around new plain functions (`do_search`, `do_agent_activity`, `do_handoff`) — the actual "one implementation, two protocol faces." New `backend/mcp_server.py`: a standalone stdio MCP server (`mcp.server.mcpserver.MCPServer`) exposing exactly `search_knowledge`, `get_agent_activity`, `handoff` as `@mcp.tool()`-decorated wrappers calling those same functions. Identity is resolved once at process startup from `MCP_API_TOKEN` (new `resolve_identity()` in `backend/app/auth.py`, extracted from `require_identity` so it doesn't need a FastAPI `Request`) — stdio has no per-call header to carry a bearer token, so one server process = one authenticated session, same shape as how these integrations get configured in practice (decisions-log.md). Verified with a real MCP client (`mcp.client.stdio.stdio_client` + `ClientSession`, spawning `mcp_server.py` as an actual subprocess — not calling the Python functions directly): all 3 tools register; `search_knowledge` and `get_agent_activity` respect the same visibility rules as the REST endpoints (confirmed a private record authored by someone else is excluded); `handoff` returns the record on success, and comes back as a clean `is_error=True` tool result (not a crash) for both "session doesn't exist" and "session exists but not visible to this identity." Caught two real SDK-behavior facts by inspecting actual objects rather than assuming: `CallToolResult`'s attributes are `structured_content`/`is_error` (snake_case), not `structuredContent`/`isError`; a tool whose return type annotation is a bare `list[...]` gets auto-wrapped as `{"result": [...]}` in structured output, since MCP structured content must be a JSON object at the top level. Also caught two test-data mistakes in my own test (both records had accidentally been authored by the same `user_id`, so the "own content" visibility rule made the private-record test meaningless) and fixed the test, not the app. All test data cleaned up; confirmed via `pg_stat_activity` that the MCP server's own `asyncpg` pool closes cleanly on client disconnect, no orphaned connections. | `mcp` 2.0.0 (official Python SDK) | [`backend/mcp_server.py`](../backend/mcp_server.py), [`backend/app/serving.py`](../backend/app/serving.py), [`backend/app/auth.py`](../backend/app/auth.py) | `record_insight`/`announce_task` (named in `architecture.md` §4) are deliberately not built here — the requested build order scoped step 6 to wrapping step 4's three endpoints only; `record_insight` would wrap ingest (step 2) and `announce_task` has no REST endpoint at all (see the step-4 decisions-log.md entry on `agent_activity`). |

## How to update this doc

- **Every time you build something real**, add a row to §3 with: date, which component it is, a one-line description of what got built, the exact tech/library (with version), and where it lives (file path or PR link).
- If that work also **locks in a stack choice** that was previously "Open" in §1, update that row's Status to "Built" (or "Decided" if chosen but not yet coded) and fill in the Tech column.
- If the choice involved a real trade-off (e.g., picking Postgres over a dedicated vector DB, or one language over another), record the reasoning in `decisions-log.md` and just link to it from here — don't duplicate reasoning in this doc.
